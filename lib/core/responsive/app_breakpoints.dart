// Centralizes viewport ranges so responsive decisions remain consistent.
abstract final class AppBreakpoints {
  static const tablet = 700.0;
  static const desktop = 1024.0;
  static const contentWidth = 1200.0;
}
