// Defines the small, consistent color palette used across the portfolio.
import 'package:flutter/material.dart';

abstract final class AppColors {
  static const background = Color(0xFF090D16);
  static const surface = Color(0xFF111827);
  static const surfaceLight = Color(0xFF182235);
  static const textPrimary = Color(0xFFF4F7FB);
  static const textSecondary = Color(0xFF9DACC3);
  static const accent = Color(0xFF66E3C4);
  static const accentDark = Color(0xFF1C9C83);
  static const border = Color(0xFF2A3850);
}
